#!/usr/bin/env bash
DOTFILES_FOLDERS="
  bin,
  lvim, 
  tmux,
  git,
  nvim,
  zsh,
  lazygit,
  alacritty,
  starship,
  kitty,
  i3,
  rofi,
  xorg,
  polybar
"

for folder in $(echo $DOTFILES_FOLDERS | sed "s/,/ /g"); do
	stow -vSt ~ $folder
done
