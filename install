#!/usr/bin/env bash
DOTFILES_FOLDERS="
  kitty,
  lvim, 
  tmux,
  git,
  yabai,
  skhd,
  hammerspoon,
  nvim
"

for folder in `echo $DOTFILES_FOLDERS | sed "s/,/ /g"` 
do
 stow -vSt ~ $folder
done

